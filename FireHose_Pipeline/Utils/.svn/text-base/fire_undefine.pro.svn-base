;; Sets the value of the input variable to undefined (taken from http://www.dfanning.com/tips/variable_undefine.html)
PRO fire_undefine, var
	if is_undefined(var) EQ 0 then var1 = size(TEMPORARY(var))
	RETURN
END
